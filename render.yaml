services:
  - type: web
    name: webdis
    repo: https://github.com/SANDIPOP999/Hip-API
    plan: free
    branch: main
    runtime: node  # specify the correct runtime here
    startCommand: node lib/api.js  # specify the start command here
    buildCommand: yarn
